// Intentionally empty by default.
// Add Drizzle tables here when the site actually needs a database.
// The gallery stores objects in the browser, so nothing here is required yet.
// See https://orm.drizzle.team/docs/get-started/d1-new for the D1 setup.
export {};
